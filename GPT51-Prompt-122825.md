Hi please create a agentic ai system that will help FDA officer to conduct medical device 510(k) review. Primary tasks:1. User provide medical device name, FDA 510(k) number etc information. Then agent will search FDA related information. Then agent will summarize (user can modify prompt and select models) results in markdown in 3000 ~ 4000 words with at least 5 tables. User can modify the results in markdown or text view 2. Then user provide pdf, then agent will transform pdf   (user can select models) into markdown (user decide what pages to transform, default first 10 pages). User can modify the results in markdown or text view. 3. Then please create a comprehensive summary  (user can modify prompt and select models) of the doc in markdown in 3000~4000 words and 20 entities with context, comments in a table. User can modify results in markdown or text view 4. Then user will provide pdf (combined two versions of doc). Then system will identify 100 differences between two doc in a table with title, differences, reference pages, comments. Then user can keep prompt on the doc ( (user can select models). User can also select agents from agents.yaml to execute on the doc. 5. Then user can paste or upload review guidance (text, pdf, md), then agent will create a review checklist based on the provide information. User can modify the results in markdown or text view. Then user can paste of upload review results (text, md). Then system will create a review report based on previous modified review checklist and user provided review results. 6. Please add additional AI Note Keeper feature/tab that user can paste text/markdown, then system will transform note into organized markdown. User can modify results in markdown or text view. Then User can use AI Magics (5 AI features created by you including AI Formating, AI Keywords that user can select color for keywords). Please also create a WOW UI that user can select light/dark themes, English/tranditional chinese, 20 styles based on famous painters (user can use jackpot to select styles). User can provide api key on webpage if not get from the environment. Don't show api key if get from the environment.
